{
  "name": "novel",
  "private": true,
  "scripts": {
    "changeset": "changeset",
    "publish:packages": "changeset publish",
    "version:packages": "turbo build && changeset version",
    "build": "turbo build --filter=./apps/web",
    "dev": "turbo dev --filter=./apps/web",
    "format": "turbo format --continue --filter=./apps/web",
    "format:fix": "turbo format --continue -- --write",
    "lint": "turbo lint --continue --filter=./apps/web",
    "lint:fix": "turbo lint --continue -- --apply",
    "clean": "turbo clean --filter=./apps/web"
  },
  "dependencies": {
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.27.1",
    "turbo": "^1.9.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.7.2",
    "postcss": "^8.4.27"
  }
}